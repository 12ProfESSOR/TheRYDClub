import 'package:flutter/material.dart';

class BusinessOppurtunities extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: (ListView(
        children: [
          Text('Growth'),
        ],
      )),
    );
  }
}
