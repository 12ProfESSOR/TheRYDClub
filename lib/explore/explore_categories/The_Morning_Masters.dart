import 'package:flutter/material.dart';

class TheMorningMasters extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: (ListView(
        children: [
          Text('The Morning Master'),
        ],
      )),
    );
  }
}
